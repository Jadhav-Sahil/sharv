<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oh No!</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #ffe6e6, #ffd6d6);
        }

        /* Circular dotted container */
        .circle-container {
            width: 320px;
            height: 320px;
            border: 5px dotted #ff3366;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            animation: pulseCircle 2s infinite;
        }

        @keyframes pulseCircle {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.05);
            }

            100% {
                transform: scale(1);
            }
        }

        .circle-container h1 {
            font-size: 1.8rem;
            color: #ff3366;
            margin-bottom: 15px;
        }

        .circle-container p {
            font-size: 1.1rem;
            color: #ff1a75;
            line-height: 1.5;
        }

        /* Try Again button */
        .circle-container button {
            margin-top: 20px;
            padding: 10px 25px;
            border-radius: 25px;
            border: none;
            background: #ff3366;
            color: #fff;
            cursor: pointer;
            font-size: 1rem;
            transition: transform 0.2s ease, background 0.3s ease;
        }

        .circle-container button:hover {
            transform: scale(1.1);
            background: #ff6699;
        }
    </style>
</head>

<body>
    <div class="circle-container">
        <div>
            <h1>Oh no! 😢</h1>
            <p>It seems you found someone else…<br>
                I am very hurt 💔.<br>
                But if you truly love, try again ❤️</p>
            <button onclick="window.location.href='/'">Try Again</button>
        </div>
    </div>
</body>

</html>